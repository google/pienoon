{
  "sounds": [
    // SoundDefs must be in the order specified by the SoundId enum.
    // See splat_common.fbs for the SoundId enum.

    // ThrowPie
    "sounds/throw_pie.bin",

    // HitWithPie
    "sounds/hit_with_pie.bin"
  ]
}
